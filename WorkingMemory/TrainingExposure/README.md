# Training exposure: preserve orientation while recovering motion

Stage1 only, authorized local first plus one cloud companion. Architecture stays the selected pre-update attention8400 model; no residual or class-offset calibration is installed. All model parameters, Adam moments/groups, Python/NumPy/Torch/CUDA RNG and parent SpatialStream state are preserved. This is a continuation, not retraining from scratch.

Two arms each receive4000updates×8=32,000new episodes. Each80-update cycle assigns control_10 nine updates per eight orientation/binding cells and four per two motion cells; focused_50 assigns five and twenty respectively. Only cycle composition changes. The existing global scheduler seed33973001and step8400offset remain. The exact parent renderer/cues/labels/losses/learning-rate policy stay unchanged: ordinary orientation/motion heads and sensory parameters3e-5, memory/attention/comparator/binding-head parameters3e-4, Adam epsilon1e-10, applicable decay1e-4, clipping1, fp32, full BPTT, sensory activation checkpointing, TF32off.

The actual existing sampler is family-local, not delay-cell-local. Preserving it yields shared per-family evidence prefixes across arms; the different schedules can assign different delays to particular prefix episodes. CPU checks compare rendered evidence/label hashes after removing inserted blanks over the first complete cycle. Equal motion counts at focused800/control4000 do not imply identical delay assignments or nonmotion experience.

Five validation looks at global9200/10000/10800/11600/12400,128examples per cell, freshseed53973001. Both arms first evaluate frozen8400 on those same examples. Eligibility requires no >2pp BA loss in any of ten trained cells versus parent. Among eligible candidates including parent select highest minimum motionD0/D24BA, then mean motionAUC, with earlier ties. This is a validation screen, not proof of equivalence. Healthy runs complete the fixed4000updates regardless of early scores.

Freshpaired testseed54973001,512per cell across allten trained conditions and four unseen binding-center conditions. Report parent, selected and terminal checkpoints, including terminal regressions even when parentfallback is selected. Preserve paired model/delay evidence and four-case binding-block uncertainty. A final report will include percell BA/AUC/confusions, paired changes, simultaneous one-sided preservation bounds against2pp and task-exposure learning curves; no average score hides regressions. No automatic repeated evaluation or follow-on residual experiment is permitted.

One local controlworker under new14,400second cap fromfirstGPUprofile, plus separately authorized focusedcloudworker under four hours frompodcreation. Same worker/protocol/source/pinnedscientificpackages on both hosts. Six disposable profiles pin feasibility for the already agreed4000updates; if that exposure does not fit, notify coordinator before production instead of independently reducing one arm. One independent read-only source/config review, no repeatedlaunchgates. Cloud researcher owns its singlepod/retrieval/lifecycle, local researcher owns only thelocalworker. Results are not claimed bitwise equivalent across GPU/OS platforms.
