# Additive controller feedback on the existing tasks

**Completed:** both arms added32,000 episodes, and validation selected global8400 for each. See [report.md](report.md), [paired results and intervention](analysis.json), [retention curves](retention_curves.png), and [completion receipt](completion_receipt.json). Feedback improved single-itemD24 by5.86pp over ordinary continuation, with costs on other cells; silencing feedback during the blank interval had little effect on decisions. This local allowance is closed, with no automatic further run.

This experiment changes the implementation only. The canceled multi-feature/direct-report proposal was never implemented or run. Existing SpatialComparison images, cues, binary orientation/binding objectives, four-class motion objective, cross-entropy losses, task heads, explicit comparator and sampling allocation are unchanged.

Two arms continue the same selected spatial checkpoint4400: ordinary continuation and an added32-unit recurrent E/I controller (26E/6I rates plus adaptation,64 additional state scalars). Both preserve all compatible trained weights and Adam moments by exact parameter name. New controller parameters have fresh Adam states. The complete parent sampler state, random generators and shuffled-schedule offset4400 are restored; both arms encounter matching fresh task prefixes beyond the parent, not a restart of its examples. The new checkpoint version makes this expansion explicit.

The controller receives learned projections of3×3 adaptive spatial summaries covering the entire64-channel current sensory field H and previous memory rate field R. Its own previous rate/adaptation states supply recurrence. It has no cue crop, explicit target/phase input, task-ID connection or direct path to the classifier. The existing forward task argument selects only the already established task head. All ordinary frames execute the same computation.

Controller rates update from a LayerNorm afferent projection, signed-source recurrent current, adaptation and rectification. Rates/adaptation themselves are not normalized. Initial rate timescales span2–8 and adaptation16–64 frames, with modest0.05 adaptation gain. E and I recurrent magnitudes each sum to0.6 per postsynaptic unit. Feedback uses signed controller rates, nonnegative64×32 output weights and learned positive32×13×13 spatial bases (each basis normalized to mean1). A positive global coupling initialized0.01 scales this ADDITIVE input to the existing spatial E/I drive; it is a learned coupling strength, not a per-frame hold/write/forget gate. Small nonzero initialization avoids a dead gradient; its actual initial logit deviation is recorded in model_checks.json. Controller and memory updates use previous controller and memory states synchronously. No predetermined orientation attractors or oracle gating are added.

The spatial rate/adaptation fields stay64×13×13, and the old-memory/current-field comparator and output route stay unchanged. All learned components may adapt. Existing LR policy remains3e-4 for memory/comparison components and3e-5 for sensory/readout, with3e-4 for new controller parameters, Adam epsilon1e-10, norm clipping1 and the same weight-decay exclusions. Full BPTT includes the sensory encoder and both recurrent systems; fp32, TF32 off, two CPU threads and one GPU worker.

The exact80-update shuffled cycle contains9 updates for each of the eight single/binding×D0/4/12/24 cells and4 for each L8 motion D0/24 anchor. Target4,960 additional updates /39,680 fresh episodes per arm, reduced equally in80-update blocks before production only if measured costs require it. Four validation looks select by the existing equal mean AUC of eight primary cells, with earlier ties; motion remains descriptive. Fresh validation seed45973001 and test46973001 are fixed. Final512-per-cell examples are paired across models and delays, including separate held-out-center binding. An unchanged-parent evaluation uses those same examples. Binding uncertainty resamples independent four-case blocks.

One secondary intervention disables the learned feedback ONLY during the24 inserted blank frames of each standard task, after selecting the trained feedback checkpoint. Nothing is disabled during ordinary training or inference. The unchanged samples/query/probe and weights are paired with its normal evaluation. This acute intervention is outside its training distribution; a deficit may show functional reliance on feedback but cannot uniquely establish an attention mechanism, and the controller itself adds memory capacity. No additional probe campaign or teaching changes are included.

One new14,400-second local ledger covers both arms, profiling, validation, held-out/parent/intervention evaluation and reporting. It starts at the first GPU profile, has no automatic renewal, and is independent of closed prior ledgers. Profile updates are recorded separately and discarded as production initialization. Actual equal exposure is pinned before production. Healthy early chance performance does not trigger a restart or capacity claim. Prior files/checkpoints remain immutable.

## Actual launch

Started2026-09-13 at12:44:44PDT; hard stop16:44:44PDT. Profiling fixed4,000 additional updates /32,000 fresh episodes per arm, for global checkpoints5,400/6,400/7,400/8,400. Each arm continues exactly50 balanced80-update cycles:3,600 additional episodes per primary cell and1,600 per motion cell. Feedback runs first, ordinary continuation second. The initial conservative estimate is12,268s training plus1,870s evaluation/report reserve; this is a finite resource calculation, not a sufficient-learning claim. The supervisor, source hashes and initial live worker are recorded in launch_receipt.json. Model totals are528,538 unchanged and574,075 with controller. The accepted focused CPU check and pre-GPU layout correction are retained in model_checks.json and cpu_check_attempts.json.
