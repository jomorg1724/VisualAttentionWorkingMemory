# Local motion-only continuation

This run asks whether focused further training can teach the current model the cued motion-duration task. It branches from the fully retrieved, hash-verified **restored-bias five-task checkpoint10000**, SHA256 `35281f264131e01678ab5725a5b66816d47302583f78b9751f775f18adf22942`. That checkpoint has seen12,800 motion episodes in the five-task run. The cloud run continues independently without changes.

Every model tensor and all133 compatible Adam states are restored. The original learned source and locality biases stay trainable. The motion-family generator state, pending counterbalance cases, global RNG and original motion delay scheduler are continued exactly. The branch therefore uses the same next motion-example prefix as the cloud branch at matching added motion-update counts. There is one batch of eight motion episodes per local update, across unchanged D0/4/12/24; no other task is rendered or trained locally. Unused output heads receive no gradients or optimizer updates. All parameters used by motion remain trainable.

The local objective is the full mean motion cross-entropy. The cloud objective is the mean of five task losses, with motion contributing one fifth before gradients are combined. Adam state, learning-rate groups, epsilon, weight decay, clipping at1, fp32 and full BPTT are unchanged; no compensating LR change is made. A gain would support targeted acquisition under this policy, not uniquely diagnose multitask gradient interference. Comparisons to cloud trajectories must use motion-specific exposure, not total five-task episodes. The cloud and local validation seeds differ; any trajectory comparison is unpaired unless explicitly re-evaluated in a later authorized experiment.

One local GPU worker uses at most two Torch/OMP/MKL/OpenBLAS threads. A new7,200-second allowance starts with profiling and includes baseline evaluation, training, final evaluation and reporting. The target is4,000 additional updates /32,000 motion episodes; measured cost fixes any lower exposure before production, with no automatic extension. Profile updates are discarded. Five scheduled validation looks use128 examples per delay, seed71973001. Selection maximizes minimum delay BA, then mean delay AUC, with earlier ties and the untouched parent eligible as fallback.

Fresh final evaluation uses seed72973001 and512 paired base episodes at each delay, comparing parent, selected and terminal models. Report all four delays and class confusions. These are2,048 scored presentations of512 base episodes, not2,048 independent histories. The saved-score report computes class-stratified paired bootstrap intervals. The supervisor enforces the original absolute local deadline and writes checkpoint hashes, lineage, exposure counts and an exit receipt. Current run discovery: `local_run.json`.
