# Dense versus spatial E/I memory with explicit comparison

**Completed:** both arms trained for4,400 updates /35,200 fresh episodes, and validation selected each terminal checkpoint. See [report.md](report.md), [paired results](analysis.json), [preserved parent reference](parent_reference.json), and [completion receipt](completion_receipt.json). Spatial binding D24 reached93.36% versus49.61% for dense, including a strong result at unseen centers, but motion performance declined and native single-item D24 remains unresolved. The allowance is closed; unused time does not authorize another run.

This is a practical two-model competition, not a test isolating locality. Both reuse the trained sensory pathway from Retention14800. Dense also retains its trained128-input/256-rate E/I core and memory-output projection; spatial receives a new64-channel local afferent normalization,64→64 pointwise input,51E/13I signed-source3×3 recurrent convolution, and64×13×13 rates plus equally shaped adaptation. E/I magnitudes initially sum to0.6 each per output over channels AND kernel offsets. Zero padding defines finite visual boundaries. Rates/adaptation are never centered or normalized. Timescales and adaptation retain the documented bounded formulas; spatial starts with tau_r2–8,tau_a16–64,gain0.05. Dense keeps its learned values.

Every physical frame compares OLD rate state with its current sensory input before updating memory. Dense uses `[old r256,current z128]→Linear128→SiLU→Linear128→SiLU`. Spatial uses `[old R64,current H64]→1×1conv64→SiLU→3×3conv64→SiLU`, then mean/max pooling to128. A common-shape128→128 projection supplies a small nonzero residual to an analogous current sensory/current-memory output route in both arms. The common residual projection starts identically. The comparator has no phase flag, cue-derived gate, memory freeze or attention. It runs every frame, and the final output is supervised. H includes opponent sensory history; it is not a purely current-image representation. Dense and spatial comparator geometry and state capacity differ and are disclosed.

Existing motion/orientation heads and sensory readout tensors are retained. A new binding head copies the orientation head's initial weights, then trains independently with fresh optimizer state. All learned sensory/input/core/comparison weights are trainable. Compatible Adam histories are transferred by exact parameter name and shape, including individual counters; spatial's incompatible core/input/output are excluded. New parameters get fresh Adam state. New task-local streams are explicit protocol migration, not bitwise continuation of the previous sampler. Adam uses LR3e-4 for memory/new comparison components and3e-5 for retained sensory/readout components,epsilon1e-10,clip1; weight decay1e-4 only for unconstrained matrix/conv weights, never raw signed/timescale/adaptation/norm/bias parameters.

The eight primary cells are single-item orientation and two-location preserve/swap binding, each at D0/4/12/24. Motion L8 atD0/24 supplies two anchors. Every shuffled80-update block has9 of each primary cell and4 of each anchor:90% primary,10% motion. Both models see exactly the same fresh per-task prefixes, batch8. New seeds: model31973001,training32973001,scheduler33973001,validation34973001,test35973001. Target4960 updates/39680 new episodes per arm if measured costs fit; reduce equal exposure in80-update blocks before production only. Four planned validation looks select each checkpoint by equal mean eight-primary-cell AUC; motion remains descriptive. Final tests use512 episodes/cell, paired across models/delays, plus a separate unseen-location binding test at all four delays. New binding positions/spacing/counterbalancing are described in STIMULI.md. Unseen locations are interpolation on a disjoint grid, not out-of-range extrapolation.

One sequential GPU worker, fp32,TF32 off,CPU threads2,full BPTT with non-reentrant exact sensory activation checkpointing. A new14400-second total local allowance begins at the first profile, covers both arms and all evaluation/reporting, and is never renewed. Six short training/evaluation profile cells per arm are saved separately and discarded as production initialization. Their cost remains charged. Production exposure is fixed from their measured costs with a reserve for both final tests. Source snapshots, strict versioned checkpoints, sampler/RNG/Adam state, state/encoder gradients, preclip norms, parameter counts and GPU memory are recorded.

Run `C:/Python310/python.exe -X utf8 -B WorkingMemory/SpatialComparison/sweep.py NEW_RUN_DIRECTORY` only within the existing authorization. Completed or failed ledgers cannot be reused to renew the budget. Healthy chance-level early learning does not trigger restarts or establish architecture failure.

## Actual pinned launch

Run `runs/spatial_20260913_100913` started2026-09-13 at10:09:13PDT, hard stop14:09:13PDT. Profiling fixed **4400 updates /35200 fresh episodes per arm**, spatial first and dense second. Both receive55 complete80-update cycles:3960 episodes per primary cell and1760 per motion anchor. Validation looks are1100/2200/3300/4400; earlier selected-checkpoint exposure is reported separately from terminal exposure. Measured profile peak allocation was606682112bytes for spatial and567616000bytes for dense, excluding desktop usage. Parameter totals are528538 and797474 respectively. The conservative reservation was12744seconds training plus1369seconds evaluation/reporting; this is a budget estimate, not a promise of wall time or scientific sufficiency.
