# Frozen orientation diagnostic

Selected Retention EI step14800 is frozen in evaluation mode. No model or parent optimizer updates, cloud work, or architecture changes. One local GPU extraction worker, fp32, TF32 off; CPU probe fitting uses two threads. A fresh1800-second wall allowance includes profile, extraction, fitting, held-out evaluation and reporting; supervisor reserves60seconds for final receipts. Seeds27973002/3/4 identify2048train/512validation/512held-out independent base episodes. Every base is presented at delays0/4/12/24 in the same split. Repeated delays are not independent samples.

The existing one-item recall renderer is unchanged: instruction + two sample rasters + D cue-marked blanks + query + probe/report. Captures are index2 (after sample),2+D (end blank, before query),3+D (after query, before probe). Analysis-only subclass records context theta without RNG draws, so actual orientation is `(theta + radians(7.5*level)) mod pi`; cos(2theta),sin(2theta) are probe targets, never model or comparison inputs. The first batch checks pixels/labels/full sampler RNG against ordinary generation; every batch checks evidence/query/probe equality across delays.

Ridge probes use r256, diagnostic-only[r,a]512, and sensory128 at each stage; identical sample states are fit once. The sensory path includes opponent traces, so it is not a current-image-only control. Each fit owns a TRAIN-only scaler. Three mean-MSE ridge lambdas1e-4/.01/1 are chosen on validation vector MSE. A fixed SiLU MLP64,150epochs,batch256,Adam.001,weightdecay.0001,validation every10epochs is added for sample-r and preprobe-rD0/D24 when that ridge's validation angular MAE exceeds3.75degrees. Angular vectors use MSE; near-zero vectors may yield unstable angles. Early-trained sample-r decoder is applied unchanged at later stages alongside their separately trained decoders.

Two operational MLP64 comparisons, D0 and D24, receive preprobe r plus independently encoded probe-image sensory features. They never receive target angle, adaptation, label or metadata as inputs. Their one-hot MSE fitting uses the same fixed recipe; D0 is a positive control, and failure at both delays limits interpretation. If a comparison improves validation BA over the deployed head by at least2pp, a ridge on existing postprobe sensory+r is fit for operational label rescue. This is not postprobe sample-orientation decoding. Independently decoded sample/probe circular distance is also classified with a validation-only threshold (maximum BA, smallest threshold tie). No further feature extractions are needed for that diagnostic.

All fits, model/epoch/lambda/threshold choices are frozen before held-out collection. Final metrics include angular errors versus7.5degree minimum change, BA/confusions/AUC, change-size counts, and paired1000-bootstrap intervals across independent base episodes shared across delays. AUC uses continuous score differences, not calibrated probabilities. Probe failure does not establish erased information, and decoder-transfer failure alone does not establish recoding. Learned probe fits are analysis-only and do not establish a biological readout or trained-system capability.

Run `C:/Python310/python.exe -X utf8 -B WorkingMemory/RecurrentComparison/OrientationDiagnostic/supervise.py` from the repository. Do not rerun without new authorization after this finite run. Scripts retain extracted features and fitted artifacts; any failure must retain its log and original deadline when repaired.
