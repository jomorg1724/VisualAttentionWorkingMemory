# Paired motion recency diagnostic

Inference only on the two selected step5000 checkpoints. `run.py` enumerates all four-direction eight-transition schedules, retaining a unique winner. Within identical count-vector, first-direction, last-two-direction and switch-count groups, it pairs the minimum and maximum temporal center of the winning direction. Fixed seed14973001 selects64 canonical pairs from each final-winner status; rotating these over four class identities gives512 independently rendered matched groups. Render seeds14983001–14983512 are new. The same128 canonical order templates underlie the512 rotated schedule pairs, a limit on generalization beyond this selected ordering bank.

Both members restore the complete same pre-render `SequenceStream` state and use its explicit-directions integration generator. No images are shuffled. Each movie has11frames: instruction,9motion images for8transitions, report. The early/late labels are relative centers, not a requirement to fall on different sides of the sequence midpoint. Actual center positions and changes are saved. The last two evidence rasters, report image and instruction are asserted exactly identical within each pair. Full movie hashes are asserted identical across model evaluations.

All model weights remain frozen, fp32, eval mode, TF32 off, two CPU threads, one sequential GPU worker. No optimizer exists. The first eight-presentation inference batch supplies the brief accounted profile; it is part of the final evaluation, not extra data. The600-second local allowance is separate from prior runs. `supervise.py` terminates its exact child at580seconds to reserve20seconds for receipts; the child also stops before analysis if it reaches570seconds.

Reproduce from the repository root with `C:/Python310/python.exe -X utf8 -B WorkingMemory/RecurrentComparison/RecencyDiagnostic/supervise.py` only with a new explicitly authorized compute allowance. This command overwrites diagnostic outputs, so preserve the existing directory before any authorized replication.

`config.json` records checkpoint/source hashes and protocol; `pairs.json` records all schedule/seed identities; `predictions.jsonl` retains paired logits, original metadata and movie hashes. `summary.json` contains means, paired delta intervals, strata and the model-by-order interaction. `exit.json` records child termination and actual elapsed compute. The report gives the interpretation.

Uncertainty uses2000 paired render-group bootstrap draws, stratified by winner and final-winner status. Both variants and both models stay inside the same resampled group. Overall class balance makes accuracy and balanced accuracy identical. Count-margin and switch strata also preserve class balance by rotating each canonical schedule. Intervals are conditional on this schedule bank; the128 reused order templates and directional rotations are not independent evidence for distinct mechanisms. Logit margin is true-winner logit minus the strongest rival; compare its order effect within model, not absolute scales between models. True-class probability supplies a bounded cross-model comparison.

This is a new controlled ordering distribution, not a rerun of the random training/test distribution. Moving winning evidence also changes which distractors occupy those positions and may change local block structure despite matched global switch count. An ordering effect therefore supports temporal-order sensitivity; it does not isolate leak, adaptation, saturation or another unique mechanism, or explain the entire original model gap.
