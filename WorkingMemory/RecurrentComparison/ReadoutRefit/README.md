# Existing readout fitting, frozen E/I dynamics

The StateDiagnostic found early-count information accessible from final firing rates and a9.47-point held-out duration gain from a linear sensory+r probe. This follow-up tests whether fitting the existing readout improves standard task performance. No slow excitatory state, attention, nonlinear adapter or new architecture is added.

Run: `runs/readout_20260912_220701`. The pinned exposure is **4840 additional updates ×8 =38,720 fresh episodes**,121 complete cycles of the original40-update six-cell allocation. This adds1936 episodes to each sensory anchor and8712 to each of the four integration/recall cells. It starts at EI step5000 (40,000 prior episodes) and ends at step9840 (78,720 cumulative). Four planned validation looks are6210/7420/8630/9840; selected checkpoints may therefore have less than terminal exposure. Exact checkpoint counters retain this distinction.

The new3600-second local ledger starts at1789276023.7671545 and ends at1789279623.7671545. The conservative six-cell profile estimated3059.17seconds for production plus438.70seconds reserved for evaluation/reporting, leading to4840 rather than the requested target5000 additional updates. This is a resource allocation, not an acquisition-sufficiency claim. Faster subsequent execution does not increase the pinned exposure. The separate profile made48 output-training and48 inference presentations; these are accounted costs outside the38,720 production episodes, and its fitted weights are not production initialization.

Only `memory_output` (32,896 parameters), motion head (516) and orientation head (258) train: **33,670 total**. Everything upstream, including encoder, memory_input, recurrent firing/adaptation dynamics and existing sensory feature computation, remains frozen in eval mode. Per-frame processing remains causal; exact fixed upstream inference replaces unnecessary BPTT. Original learned output weights and compatible Adam moments/steps are preserved. New protocol version is `ei_readout_only_continuation_v1`; continuation checkpoints require exact new config/source compatibility. Parameter inventory and optimizer grouping are unchanged; original moments are restored and shape-checked. The parent sampler and RNG state resume their next fresh examples, not a new seed/restart.

The original LR grouping remains: memory-output projection0.0003, inherited task heads0.00003, Adam epsilon1e-10, matrix-weight decay0.0001, gradient clip1. No decay is added to biases. Log preclip norms, clipping and actual projection/head gradients and relative updates. The whole model stays eval mode; the trained modules are linear. All frozen parameters are compared exactly against their original tensors in the profile and at each training checkpoint boundary. The first profile batch verifies the detached feature route matches existing forward logits exactly.

Validation uses128 fresh episodes per each of six cells, seed18953001; the maximum equal six-cell mean OVR-AUC selects the checkpoint, with exact ties favoring earlier. Final evaluation uses512 per cell, seed19953001, shared between the untouched EI5000 parent and selected refit. Both anchors and orientation recall are retained. This is an unchanged-parent before/after test, not a comparison against equally exposed end-to-end training. Final paired analysis must retain each episode's matched metadata; repeated model views are not independent episodes.

`train.py` and `sweep.py` reuse the prior worker/supervisor/evaluator structure. All jobs run sequentially, fp32, TF32 off, CPU threads2. The supervisor reserves60seconds before the overall deadline for receipts. Immutable full-state checkpoints include optimizer/sampler/RNG/cumulative exposure; run source/config hashes and source copies are pinned before launch. `results.json` and the run aggregate reflect actual progress; completion requires final test results and an exit receipt.

Single read-only review: `/root/temporal_neuroscience_design`. The reviewer found an inherited-counter resume bug during implementation; replacing counters from the checkpoint, rather than adding parent counts again, fixed it before source pinning and the first profile. No profile or training rerun was needed. Review details are in `decision_review.md`.
