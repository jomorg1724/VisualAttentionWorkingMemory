# Single actual-source review

Reviewed `model.py`, `train.py`, `remote_sweep.py`, `README.md` and saved `model_checks.json`. No substantive implementation or protocol issue prevents the authorized fixed-exposure run. No additional tests or review cycle are requested.

The joint attention dimensions agree: 169 prior-memory queries, 338 current-sensory/prior-memory keys and values, two 32-dimensional heads, and a 64×13×13 output. This output alone enters the inherited local normalization and E/I input convolution. The original comparator still reads previous rates and current sensory H; the classifier keeps its inherited routes. There is no extra controller, persistent attention cache, raw-H memory-drive bypass, phase mask or new loss.

Query positional embeddings avoid identical zero-memory queries at startup. Q/K normalization does not normalize the raw values, and identity value/output initialization preserves their coordinate scale. Learned source and locality preferences use grid-cell distances, as intended. This is a near-local, sensory-favoring initialization, not an exact functional continuation: the saved CPU check reports maximum parent logit deviation 0.16825 and nonzero Q/K/value/output/locality/source and early-state gradients. No explicit token mask exists, although the initial locality penalty makes distant weights numerically negligible or zero through fp32 underflow. Global routing can become useful only as its learned scores/preferences change.

Every frame updates the same computation from current H and previous R; recurrent and attention computations remain on the full BPTT graph. Existing E/I sign constraints remain in the inherited core; the unconstrained attention projections do not constitute a globally Dale-constrained circuit. H still includes opponent temporal traces, so it is not an independent instantaneous image encoding.

Teaching and continuation match the local controls: inherited SpatialStream, heads, classification cross-entropy, exact 80-update 90/10 schedule, parent sampler/RNG state and global offset 4400. Compatible Adam states transfer by name (111 exact states in the saved check), new attention states start fresh, counters restore without additive duplication, and learning-rate/decay/clipping policies match. The portable initializer preserves actual CPU-created weights; its enclosing manifest verifies file hashes before execution.

The remote supervisor fixes 4,000 additional updates / 32,000 episodes at batch 8, four validation looks at 5400/6400/7400/8400, and the same validation/test seeds and selection rule as the local controls. Profiles are discarded. The absolute manifest deadline includes setup time; a cost estimate that cannot fit causes a stop rather than silently reducing exposure or extending the allowance. Worker deadlines reserve retrieval time. Pod retrieval/deletion remains the coordinator's lifecycle responsibility, as documented.

Interpret this as a practical comparison of pre-update attention under unchanged teaching, with 27,590 added learned parameters and no additional persistent state. It does not independently isolate biological attention, and selecting stored content does not itself guarantee preservation through the subsequent rate/adaptation update. Paired task comparisons should retain binding four-case block uncertainty and disclose cross-platform differences and primary-only checkpoint selection.
