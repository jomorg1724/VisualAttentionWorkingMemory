# Single frozen-diagnostic review

Reviewed actual `diagnostic.py`, `common.py`, `check.py`, `checks.json`, `run.py`, `analyze.py` and `README.md`. No substantive issue prevents the authorized diagnostic. No additional tests, training or review campaign is requested.

- The checkpoint loader restores selected attention8400 and continuation8400 exactly and disables gradients in evaluation mode. There is no optimizer or fitting stage. The saved checks show exact ordinary/wrapped and empty-phase baseline equality, excluded memory-source mass zero, unchanged sensory branch, and unchanged comparator for probe-only exclusion.
- Orientation sample frames 1–2, blanks 3 through 2+D, identity query 3+D, and probe 4+D match the existing task. Motion transition-bearing frames 2–9 exclude the initial reference and report. The interventions operate on source logits or the drive supplied to inherited memory normalization; they do not alter images, labels, probes or the ordinary trained source.
- Memory K/V exclusion preserves memory-dependent queries and the native recurrent/adaptation path. Visual weights renormalize after exclusion. Therefore this is a conditional routing intervention, not removal of all remembered information. Motion H-drive bypass removes attention from that input path on the selected frames while preserving the inherited comparator and sensory output routes.
- Caching each frozen model's sensory H sequence is valid because neither model feeds memory back into sensory computations. Each intervention recomputes its own evolving memory state. Probe-only intervention changes the postupdate memory route but cannot change the comparator already computed from previous memory and current probe H.
- Both models and all intervention variants use the same examples, and D0/D24 restore identical evidence streams. The previous held-out metadata/labels are checked against the regenerated episodes. Analysis uses shared class-stratified bootstrap indices for paired accuracy differences and does not multiply independent sample size across variants/delays. This is reuse of an existing held-out set for diagnosis, not a new generalization test or training selection.
- The 1,800-second ledger starts before GPU placement/profiling and bounds inference plus analysis. Disposable profiles determine a fixed prefix of at most 512 primary episodes, with a D0 orientation anchor of at most 128 and a reporting reserve. No cap renewal, new cloud worker or model updates are present.

Final branch contributions reconstruct logits with classifier bias counted once. They are descriptive projections of the resulting features, not branch-removal causal effects. Source value RMS is measured per head before the learned output projection; source mass/RMS alone does not measure retained task information. Any acute effect remains conditional on the frozen trained system and intervention-induced visual renormalization; failure to rescue is not evidence of irreversible erasure.
