# Visual Attention and Working Memory

This repository was reset at the user's explicit request. Do not recover previous implementations, plans, task boards or experiment results.

The project builds components of a visual attention and working-memory system, using Jeremy Wolfe's Guided Search6.0 as a functional scaffold. Omit its diffuser. For now, treat activated long-term memory as synaptic weights; this is the user's modeling approximation, not a claim made by Wolfe.

PreAttentiveVision (PAV) is only the first component. Visual working memory, visual attention and integration follow later. Do not equate the whole project with PAV or select their implementations now.

Current direction (2026-09-16): the previous lineage is closed as a failure; see HANDOFF.md. Keep the five-task spatial battery as the benchmark, audit the environments and training logic for bugs and mis-specification first, then re-evaluate the architecture from first principles with every parameter trainable and a plain baseline beside every component. Never freeze weights or inherit a learning-rate split during development. No training or cloud provisioning is authorized by this note.

Historical first task (complete): research, implement and compare five promising lightweight convolutional encoders, each processing two separate100x100x3frames through shared weights; a sufficiently expressive common decoder predicts change/no change. Include several stimulus families such as Gabors, moving dots and images. Put all PAV tasks, code, source rationale, results and discussion in PreAttentiveVision/.

Researcher agents implement and execute experiments. Use primary literature/official implementations, explain adopted mathematical properties, and distinguish measured performance from neuroscience grounding. Run GPU jobs sequentially with a finite local budget; no paid/cloud compute or unrequested external publication. The current user instruction controls scope and supersedes historical plans. No mandatory architecture, obsolete harness, repeated validation campaign or old roadmap is inherited.

Current correction: exactly two frames remains the constraint. Use random-dot motion grounded in Krauzlis's experiments, with one of four cardinal mean directions; classify left/right/up/down. This replaces the dots change/no-change objective. The initial single-dot-displacement benchmark was stopped and is superseded; preserve its checkpoints as historical diagnostics only. Read PreAttentiveVision/krauzlis_stimulus.md for primary-source methods and disclosed adaptations. Research complementary neuroscience tasks suitable for two-frame encoder evaluation. Direction decoding must retain frame order; a swap-symmetric pair decoder loses the sign of motion. PAV does not yet implement attention selection or working-memory delays.

The user has authorized training and exploration of the recommended seven-task battery: motion direction, signed orientation, contrast, spatial frequency, chromatic increment, contour grouping, and natural-image spectral detail. Implement the documented tasks and compare all five existing candidate encoders with the same ordered decoder and task-specific outputs. Run sequentially on the local GPU within the remaining 3057.7762 seconds of compute allowance, including profiling and evaluation. Fix equal per-model training exposure after measuring throughput; report each task separately. Researcher agents execute; do not add approval gates or renew the compute cap. This is an initial empirical comparison, not a sufficient acquisition horizon or a biological validation.

The three-arm hybrid experiment is complete; neither addition met the contour-improvement criterion. Preserve its trained late-SE checkpoint as a candidate, including its strong motion decisions.

Current authorized experiment: execute PreAttentiveVision/next_experiment_task_allocation.md. From the same late-SE step-1512 parent, compare uniform seven-task continuation with a schedule allocating half of updates to contours and one twelfth to each other task. Preserve architecture, weights and Adam state; introduce explicit shared task-local fresh streams so common per-task prefixes match across arms. This is a sampling-allocation experiment, not a new architecture or a bit-for-bit replay of the old single-stream order. Use equal total additional updates, planned validation selection by minimum task BA then mean task AUC, and fresh paired final evaluation across all seven tasks. The previous completion receipt leaves exactly 1008.2650083768344 seconds of the original allowance, including new profiling, training, evaluation and analysis. Target1008 updates/arm if measured costs fit; otherwise reduce in84-update increments. No automatic budget renewal, unrelated process termination or concurrent GPU training.

Update: the allocation comparison above is completed, both arms at step2268 after756 additional updates. The contour-focused checkpoint is the current trained PAV reference. Current user request is DESIGN of three causal frame-by-frame accumulators before working memory, with one neuroscience-inspired candidate. See PreAttentiveVision/TemporalIntegration/README.md for spatial KDA, ConvGRU and opponent temporal filters. This design does not authorize another GPU run or renew prior compute allowance. Preserve the trained encoder and old pair reference; do not recover deleted historical KDA code. Prior KDA success mentioned by the user motivates a candidate but is not a verified result for this implementation.

Current authorization update: the user subsequently said "proceed with training" for those three designs. Researchers implement and execute TemporalIntegration/README.md, using the frozen selected step2268encoder, new temporal cores/readout, shared fresh task-local streams and the focused12-update schedule. Target4032 updates/model with a new finite7200-second local experiment cap including profiles/evaluation/analysis, starting at the first GPU profile. Reduce equal exposure before production if measured costs require it; do not extend automatically. This explicit new request supersedes the preceding design-only status. Actual launches/results require run evidence.

Current scope update: that temporal comparison is completed (all4032 updates; opponent98.44-100% across seven held-out tasks). User now requests DESIGN ONLY of a memory-capacity task battery, and explicitly narrows future work to the opponent winner; hold KDA/ConvGRU aside unless a later roadblock warrants revisiting them. See WorkingMemory/TASK_BATTERY.md. Preserve trained progress, plan learning of all new cues/probes/sequence rules with learned weights unfrozen, and distinguish selective integration, decision retention and retrospective item-bound sensory memory. A separate working-memory module must earn its place; no architecture or new training launch is authorized by the battery design request. Unused compute does not trigger more runs.

Current authorization: user subsequently said "Lets proceed with the training" for WorkingMemory/TASK_BATTERY.md. Researchers implement/train ONLY the opponent winner, warm-starting its selected4032 checkpoint and unfreezing all learned weights in a separately versioned sequence learner. Fixed trace/kernel/energy computations remain fixed. New finite14400-second local cap starts at firstGPUprofile, includes eval/reporting; target40,000 fresh episodes if profiled costs fit, actual exposurefixed beforeproduction. Initial sensory/cuebridge flows intojointtraining withcompatibleprogresspreserved; poor early scores are not a stop criterion or capacity conclusion. FullsequenceBPTT,fp32,oneGPUworker, newoptimizer/source/protocolrecords,noautomaticextension. No new WM module, othermodelcomparison orinterleavedarchitecture is included.

Current authorization update: the sequence battery above completed 39,200 episodes; selected checkpoint update 6,860. The user has now explicitly said "great work. Start the training run" for the LSTM versus neuroscience-inspired E/I adaptive recurrent-memory comparison, after emphasizing numerical stability and fair optimization. Execute WorkingMemory/Research/recurrent_memory_without_attention.md in WorkingMemory/RecurrentComparison. No attention, slot addressing, sensory feedback or interleaved accumulators. Use the same selected sequence parent, all learned sensory weights trainable with lower LR, per-frame opponent emissions, matched focused motion-duration/orientation single-item recall streams and sensory anchors. Researcher implements/runs; one independent read-only review of actual config/code before first production, no repeated launch gates. A new finite 14,400-second total local cap covers BOTH arms, profiles, evaluation and reporting; target 40,000 fresh episodes per arm if measured costs fit, otherwise pin a reduced equal exposure before production. This new allowance does not reuse or extend old ledgers. Full BPTT, fp32, sequential GPU workers, documented normalization/initialization/clipping and state-gradient diagnostics. Preserve all checkpoints and continue healthy runs through the agreed allocation; early chance behavior is not a failure claim. No automatic further runs or budget renewal.

Execution placement update: user explicitly requested putting the neuro model on RunPod under the Palladio email account. The UI verified jonathan@palladio.ai; pod mddhcqx9eed6u7 (vawm-neuro-ei-40000, single RTX 3090, EU-CZ-1) was provisioned there on 2026-09-13 at 03:34:40 UTC, approximately $0.505/hour including its 30GB container/10GB volume. The connected MCP sees this same pod. This request authorizes this paid remote neuro run and its transfer/cleanup, not unrelated cloud work. Preserve the pinned 40,000 episodes, batch8, model/task/optimizer settings and shared initialization. Researcher runs the remote arm; LSTM continues locally under its existing deadline, with local EI removed through a reviewed external supervisor handoff. Remote setup/training/evaluation has a three-hour wall cap from pod creation (06:34:40 UTC), no automatic renewal. Retrieve evidence and checkpoints, stop the pod on completion/cap, and terminate this ephemeral pod after successful artifact retrieval to prevent ongoing storage charges. Record hardware/platform differences; do not claim bitwise cross-platform equivalence or extend episode exposure.

Current diagnostic authorization: both 40,000-episode recurrent models and tests completed; the cloud pod was deleted after verified retrieval. User said "do it" to a cheap paired recency diagnostic on the two selected step-5000 checkpoints. Researcher implements/runs WorkingMemory/RecurrentComparison/RecencyDiagnostic locally, with no training or cloud restart. Target 512 early/late matched pairs at the trained eight-transition motion length, identical direction totals/winner/margin, controlled last direction/suffix and switch count, matching first direction where feasible. Regenerate continuous movies with shared nuisance draws rather than shuffling rendered frames; verify any claimed identical raster suffix. Evaluate both frozen models on the same pairs; paired uncertainty and model-by-order interaction, label/confidence/margin statistics, no inflated independent episode count. New finite 600-second local compute allowance for profiling/evaluation, no automatic extension. Preserve all prior results; this diagnostic can identify ordering sensitivity, not uniquely establish a leak/adaptation mechanism.

Current development authorization: after the completed recency diagnostic, the user said "lets proceed with your recommendations": first test whether earlier directional evidence is recoverable from the frozen trained E/I population, then make one targeted improvement according to the finding. Researcher executes StateDiagnostic with separate probe-training/validation/test schedules, analysis-only readouts, and a new finite 1200-second local compute cap. Probe success is evidence of recoverability; probe failure alone does not establish erasure. If usable evidence remains, investigate its readout/use; if retention is limiting, consider slow excitatory synaptic dynamics with fast inhibition and existing adaptation. Do not preselect that circuit change before the diagnostic. A subsequent focused training comparison is authorized in principle, with the exact single change, continued-parent control, equal additional exposure and finite local budget to be recorded before launch. Preserve trained checkpoints and compatible optimizer state; no attention, hardcoded directional counters, unrequested architecture sweep or new cloud run. One independent read-only review of each material decision; no repeated launch gates.

Development decision: StateDiagnostic completed with accessible early-count information in final firing rates and a held-out linear-readout gain of 9.47 percentage points; its nonlinear probe did not show an advantage. Defer slow synapses and new readout architectures. The authorized follow-up is narrowed to ONE readout-only continuation in WorkingMemory/RecurrentComparison/ReadoutRefit: train the existing memory_output and motion/orientation heads, freeze all sensory/recurrent computations in evaluation mode, preserve compatible trained weights/Adam/stream/RNG using an explicit versioned training-policy migration. Target 5000 additional updates/40000 fresh episodes using the existing six-cell cycle under a new finite 3600-second local cap including profiling/evaluation/report; pin lower exposure before production only if measured costs require it. Retain existing learning rates, epsilon, weight decay and clipping, and log trainable gradients/updates. Four planned validation checkpoints select by six-cell mean AUC; fresh paired final evaluation compares the untouched parent and refitted output on all six cells (512 episodes/cell). This narrower test asks whether output-only fitting improves the deployed fixed computations. It uses the untouched parent as its baseline, superseding the earlier conditional plan for equally exposed architecture arms; it does not establish superiority to further end-to-end training or isolate a recurrent circuit mechanism. No new module, attention, full-training comparison arm, cloud restart or automatic cap extension.

Current retention authorization: ReadoutRefit completed 38,720 additional episodes, selecting checkpoint9840; long-motion BA improved70.31% to79.30% with frozen sensory/recurrent parameters. User now says "proceed" to the recommended retention experiment in WorkingMemory/RecurrentComparison/Retention. Reuse that selected checkpoint and compatible optimizer/RNG/stream progress. Compare existing L8 motion followed by D=0,4,12,24 blank frames and report, against existing one-item orientation sample followed by the same D, query and comparison probe/report. In the actual renderer post_delay is query-to-probe; fix it at0. Preserve all other images/cues and pair identical evidence/probes across delays. Measure the initial checkpoint, then train the existing E/I recurrent core, memory_output and motion/orientation heads on randomized primary delays, with sensory/opponent/memory_input fixed in eval mode. Full temporal gradients through r/a; no attention, distractors, new objects, longer motion streams or new architecture. New finite14,400-second local cap includes profile, baseline, training, evaluation and reporting; target40,000 fresh episodes if measured costs fit, pin actual exposure before production. Researcher executes with one independent read-only protocol/code review and existing run infrastructure. Preserve all progress, record fresh exposures and repeated paired presentations separately, and report baseline unfamiliar-delay performance as generalization rather than proof of retention limits. No cloud restart or automatic further run.

Current orientation diagnostic authorization: Retention completed39,680 additional episodes and selected14800; motion remained about80% across delays, while orientation comparison remained near chance at24 inserted blanks. User said "do it" to diagnostic tests of sample-information accessibility, changes in representation over time, and comparison/output use. Researcher executes OrientationDiagnostic on the frozen14800 model, with a new finite1800-second local cap including extraction, CPU fits, evaluation and report. Target2048/512/512 independent train/validation/test base episodes paired across D0/4/12/24, contingent on measured costs. Record the actual rendered axial angle through an analysis-only wrapper; random context angle makes latent stimulus level an invalid angle target. Decode pre-probe states, compare sample-trained and late-trained decoders on the same held-out episodes, and measure angular precision by actual change size. Treat firing rates as the ordinary output route; adaptation access remains diagnostic only. Fit one small operational comparator at D0 and D24 from pre-probe firing rates plus independently encoded probe images, with no privileged angles/labels as inputs. Weak absolute-angle decoding need not rule out a useful comparison representation. A post-probe label-readout fit may follow a comparator rescue; sample-angle decoding after the probe requires controls against probe/sample correlation. All original model weights remain unchanged; independent grouped splits, train-only scaling and validation-only fit selection, one source/interpretation review, preserved attempt/compute receipts. Failed probes do not establish erasure. No new architecture, attention, main-model training, cloud run or automatic follow-up.

Current spatial competitor authorization: OrientationDiagnostic completed and found useful pre-probe orientation information at D24 (4.25-degree linear decoding error; label-only diagnostic comparator 73.44% versus deployed 50.00%). The user now explicitly says "lets build it as a competitor now and run some experiments/trainings you suggest" after the spatial-memory research discussion. Researcher implements and runs WorkingMemory/Research/spatial_ei_memory.md in WorkingMemory/SpatialComparison: one spatial E/I candidate with 64 x 13 x 13 rate and adaptation fields, alongside a dense E/I competitor; both get explicit learned old-memory/current-sensory comparison before the task decision. Train on existing single-item orientation and new two-location orientation preserve/swap binding at D0/4/12/24, with a small existing motion-duration anchor allocation. Use varied trained positions and separate held-out-location evaluation, label-balanced fresh shared streams and paired final examples. Train new stimuli rather than assuming zero-shot generalization. Start from compatible Retention14800 sensory weights; retain compatible dense memory weights/Adam, initialize incompatible spatial/comparison parameters explicitly, and disclose unequal prior core experience and different state sizes. All learned components may train with lower sensory LR; no attention, oracle phase-based state gating, additional architecture sweep or cloud run. New finite 14400-second total local experiment cap begins at first GPU profile and includes profiling, both arms, evaluation and reporting; target40000 fresh episodes/arm at batch8 if measured costs fit, otherwise pin reduced equal exposure before production. Full BPTT, fp32, sequential GPU workers, one independent read-only review of actual protocol/code/config before production, no repeated launch gates or automatic budget extension. Preserve all prior checkpoints and results.

Current selective-maintenance authorization and correction: SpatialComparison is complete (35,200 episodes per arm; spatial selected4400, strong binding but singleD24 remains chance and motionD24 is weak). The user authorized testing the attention/control idea, then explicitly corrected: "Lets not adjust how we teach anything! At least not yet. We have known failures already. The implementation we should look for is one that addresses those." No proposed continuous-report/angle supervision, new cue tasks, feature curriculum or quadrant split was implemented or launched. Researcher now implements WorkingMemory/SelectiveMaintenance as an ARCHITECTURE-ONLY comparison from the SAME selected spatial4400 parent: unchanged ordinary continuation versus addition of a 32-unit E/I controller (rate plus adaptation), observing learned summaries of the full sensory field and memory and supplying additive previous-state currents to the existing E/I memory. No cue crop, per-frame hold/forget gate, phase oracle or controller-to-classifier shortcut. Keep the existing comparator, output heads, losses, stimuli, visual cues, labels and shuffled80-update90/10 training cycle unchanged; inherit compatible weights, optimizer, sampler and RNG state for both arms. All learned components keep the existing learning-rate policy, new parameters get fresh optimizer states, full BPTT and fp32, sequential GPU workers. Count extra controller state/parameters and interpret the result as a feedback-package test, not causal proof of attention independent of recurrent capacity. A new finite14400-second total local cap begins at first profile, including both arms and evaluation/reporting; target4960 additional updates/39680 fresh episodes per arm if measured costs fit, otherwise pin lower equal exposure before production without extension. One independent read-only source/config review, no repeated launch gates. Same paired final battery including unseen binding centers; one small paired delay-only feedback interruption is evaluation-only, with an out-of-distribution caveat. No new task/objective, cloud run, architecture sweep or automatic budget renewal. Preserve all earlier trained models and artifacts.
Current cloud authorization (2026-09-13): the user explicitly said "go ahead and launch that one on the runpod" for the proposed pre-update joint sensory/memory attention competitor. Researchers implement and run WorkingMemory/PreUpdateAttention on the previously verified Palladio RunPod account. This supersedes the preceding conditional/design-only status for this one alternative. Preserve the ongoing local SelectiveMaintenance experiment. Start from the SAME spatial selected4400 parent; train 4000 additional updates / 32000 episodes at batch8, keeping the existing tasks, cues, losses, comparator, heads, 80-update 90/10 cycle, compatible optimizer and sampler/RNG state and learning-rate policy. Two 32-dimensional heads use previous spatial memory queries and joint current sensory plus previous memory keys/values; attended drive replaces raw sensory drive into the E/I memory update. Preserve the spatial E/I dynamics and fixed-size state, with no new controller, blank oracle, task teaching, recurrent gate or frame-growing cache. Record position/source encoding, numerical stability, initialization and architecture migration explicitly. One independent read-only source/config review before production; no repeated gates. Use one modest 24GB cloud GPU with a new finite four-hour cap from pod creation covering setup, profiling, training and evaluation, and no automatic extension. Pin exposure before production if measured costs cannot fit and report any reduction. Reuse planned paired evaluation draws from SelectiveMaintenance, record hardware differences, and interpret this as an architecture comparison rather than biological proof. Retrieve artifacts, stop at completion/cap, and delete the ephemeral pod after successful retrieval; do not leave idle GPU billing. No extra training arm, unrelated cloud job or publication is authorized.
Current frozen diagnostic authorization: after completion and deletion of the PreUpdateAttention RunPod, the user said "Yes lets continue with your suggestions. We should also try to understand why motion suffered reduced accuracy." Researchers execute WorkingMemory/PreUpdateAttention/MechanismDiagnostic and a saved-artifact MotionAudit. Keep all main-model weights frozen and all tasks/cues/losses unchanged; no retraining, new architecture, RunPod restart or paid compute. Use selected attention8400 and ordinary continuation8400, with existing parent/feedback saved predictions as context. Test acute exclusion of memory-source attention keys/values separately during encoding, inserted blanks, query and probe/report (the native single-orientation query carries an identity cue), using actual renderer frame indices only in a diagnostic wrapper. Keep memory-derived queries and ordinary E/I recurrence intact, so this tests use of memory as the attention source rather than removal of all recurrent memory. Pair identical existing-task episodes across interventions; confirm unmodified wrapper equivalence and checkpoint immutability. For motion, test memory-source exclusion and raw-sensory-drive bypass during moving frames, analyze decision-branch contributions, confusion and available schedule metadata. Acute interventions are out of distribution; rescue implicates the changed computation, absence of rescue does not prove information erasure. Audit saved motion validation trajectories and the 10 percent training allocation/selection objective; do not treat allocation as a proven cause without a controlled training comparison. New finite 1800-second local cap from first GPU profile includes profiling, frozen evaluation and reporting; target 512 paired test episodes per diagnostic cell if profiled costs fit. Use one sequential local GPU worker, bounded CPU analysis, one independent read-only source/protocol review and no repeated gates or automatic budget renewal. Preserve all trained progress and earlier artifacts. Analysis-only probes, if justified within this cap, require separate fitting/selection/test splits and must not modify deployed models.
Current training-exposure authorization: the user said "go ahead and proceed with training locally first. If you need to run an additional experiment start the runpod so we can run simultaneously." Researchers now execute Stage1 of WorkingMemory/Research/training_exposure_and_temporal_residual.md in WorkingMemory/TrainingExposure. Start the unchanged10%-motion continuation locally first; run the matched50%-motion allocation arm concurrently on one Palladio RunPod RTX3090 (or comparably priced single24GB GPU). Both start from the identical selected attention8400 checkpoint, preserve architecture, tasks/cues/losses, all compatible Adam and RNG/task-local stream progress, inherited learning rates and full BPTT/fp32. Target4000 added updates /32000 fresh episodes per arm at batch8. Use complete80-update cycles (control9 per primary cell and4 per motion delay; focused5 per primary cell and20 per motion delay), fresh shared validation/test seeds and baseline evaluation, and planned800-update validation looks. Report total-update and task-exposure curves separately. Preserve every task in per-cell results; selection uses the documented2pp validation preservation screen and balanced motion criterion with parent fallback, not an aggregate that conceals regressions. Cross-platform differences are disclosed. A new finite14400-second local cap begins at the first GPU profile for the local arm; the cloud arm has a separate four-hour wall cap from pod creation including setup/evaluation. Neither cap renews automatically; no unequal exposure reduction after production starts. Use one independent actual source/config review, no repeated launch gates. Retrieve and verify cloud artifacts, stop on completion/cap and delete the ephemeral pod after successful retrieval. No other cloud job, simultaneous local GPU worker, learning-rate sweep or residual training is included in this Stage1 launch; inspect its results before deciding Stage2. All prior checkpoints remain preserved.

Lab journal: The user requested an extensive wiki-style research journal in LabJournal/. Researchers maintain it alongside experiment reports: document the question, implementation and training changes, checkpoint lineage, actual exposure, per-task results, interpretation, failures, and next decision with links to supporting artifacts. Keep its index/current status and chronological record current after completed experiments or material findings. Clearly label live validation snapshots and separate them from final held-out results. Preserve the post-reset scope and do not recover deleted project history. Documentation must not add GPU work or launch gates.

Current latent-state analysis authorization: the user requested visualization and possible segmentation of working-memory latent states, including orientations, changes, cues, motion direction and evidence accumulation. A researcher implements WorkingMemory/LatentDynamics using frozen attention8400, unchanged trained tasks and analysis-only metadata/probes, with PCA and bounded UMAP/t-SNE views, spatial/channel tuning and temporal generalization. Prepare while existing training runs; one local extraction worker starts only after TrainingExposure's local GPU work finishes, without cloud restart or extending an existing pod. Use a new finite 1800-second analysis compute cap from the first profile/extraction including fits and rendering; pin feasible episode counts after profiling, CPU threads at most2 (1 while training active). Keep paired episodes grouped across times/delays, actual axial angles, train-only fitting and explicit cue/phase and motion recency/time confounds. No deployed-weight updates, new stimuli/teaching, architecture changes, mandatory additional review gate or automatic budget extension. Save reproducible features, figures, a scientific HTML atlas and a report, and update LabJournal. The initial analysis targets attention8400; an additional completed Stage1 checkpoint may be described only if explicitly recorded, available and within this same cap.

Current attention visualization correction: user said to hold off on model experiments and asked for attention maps averaged over trial conditions and projected over scenes, separately for immediate visual and memory sources. Pause the broader LatentDynamics extraction/probe experiment; supervisor4644 was stopped while queued and no analysis budget had started. Researcher now prepares frozen attention8400 forward-only visualization in WorkingMemory/AttentionMaps using unchanged existing trials, head-by-source maps, source allocation, condition averages and scene overlays. Reuse the unspent1800-second analysis allocation, waiting for existing TrainingExposure evaluation/GPU workers to exit. Both heads attend both sources; visual/memory are key banks, not fixed head identities. Preserve joint attention mass and label within-source normalization; remembered-scene overlays are spatial references, not pixel attribution. No deployed-weight updates, new tasks, interventions, probe-fitting experiment, cloud restart or additional architecture run. Keep broader latent analysis on hold; update journal and monitor to reflect this scope.

Attention visualization display correction: the user explicitly rejected temporal averaging and requests per-timestep maps for every task. The attention researcher now captures every frame of eight existing trials per family at delays0/24 from the same frozen attention8400 checkpoint, using only the original remaining AttentionMaps wall allowance ending2026-09-14T01:21:53.341Z. Preserve exact individual-trial maps and actual current frames, including initial motion reference. Default viewer must show one trial at one timestep with four visible13x13grids (eachhead x visual/memory), playback/frame slider and consistent0-to1 source-allocation scale. Old phase-average products remain historical and must not be presented as per-timestep. No training, interventions, broader latent probes or new budget.

Current bias-removal and spatial-battery authorization: the user explicitly requests training the model without the added attention biases on RunPod, both on the new spatial task battery and on the currently demonstrated tasks for comparison. This supersedes the preceding experiment pause for these two runs only. Researchers implement WorkingMemory/UnbiasedAttention and WorkingMemory/SpatialTaskBattery. Remove the explicit source_bias and softplus(raw_locality) distance penalty from attention logits; retain trained Q/K/V/O, source and position embeddings, normalization, sensory computations, E/I memory and comparator. No Q/K reinitialization. The old-task arm starts from attention8400 with compatible weights, Adam, RNG and streams, follows the exact completed TrainingExposure control_10 recipe for 4000 added updates/32000 episodes, and compares against its preserved biased control using the same evaluation draws and selection; disclose reused-test exploratory status and hardware differences. The new arm starts independently from that same bias-free migrated parent, with five fresh semantic heads and streams: four-location signed orientation change using the existing significance-sign glyph, cued multi-patch motion duration, a documented Krauzlis cued target/foil motion-change task, spatial orientation binding, and exact scene-set recognition (0/4/12/24 study frames, three blanks, query held three to five frames). Preserve 100x100 RGB inputs and report per condition. Recognition source images are split by identity; empty sets are always negative and scored separately. One 48GB cloud GPU runs the two arms sequentially, with a new finite eight-hour wall cap from pod creation including setup, profiling, evaluation and retrieval. New-battery target is 4000 updates with five task microbatches of eight episodes per update (160000 episodes), averaged losses and one clipped Adam step, full BPTT/fp32 and inherited learning-rate policy; pin feasible new exposure before production from profiling while keeping the old-task comparison exposure fixed. Do not extend the cap, start extra arms, change tasks midrun or restart paused latent analyses. Record actual exposure, checkpoint lineage, removed terms, all task results and limitations in reports and LabJournal. Retrieve and verify artifacts, stop at completion/cap, and delete the ephemeral pod after successful retrieval. No repeated launch review gates.

Current cancellation: the user said "no need. Kill the 5 task battery you started". Stop the active SpatialTaskBattery training and its supervisor; do not resume it, launch another arm, or renew its budget. Preserve the completed old-task bias-removal comparison and existing artifacts. Retrieve remaining saved artifacts, stop and delete the ephemeral pod after retrieval, pause the training heartbeat, and record the new arm as user-cancelled rather than completed or a measured capacity failure.

Current restored-bias training authorization: the user subsequently instructed keeping the original attention bias terms, restoring them and launching training on the pod again. Keep the cancelled bias-free five-task run as a separate cancelled attempt. Researcher now trains the SAME five-task battery with both original source_bias and softplus(raw_locality) distance terms intact and trainable, starting independently from the original biased attention8400 parent with its compatible optimizer state and fresh five semantic heads. Preserve learned Q/K/V/O, position/source embeddings and all other inherited components. Preserve the five-task stimuli, fresh initialization/streams, five microbatches of eight with averaged losses, learning-rate policy, full BPTT/fp32, selection and reporting; no old-three-task rerun. Reuse the healthy existing L40 pod ep8bmcjxz9k66l. Target 4000 updates/160000 episodes within the ORIGINAL 2026-09-14T09:36:25.001Z deadline, profiling only to confirm fit or pin a lower exposure before production; no automatic cap renewal. Use a separately versioned run with source/config/lineage evidence; do not overwrite the completed old-task comparison or cancelled bias-free training. No extra approval gate. Resume the monitor for this restored-bias run and retrieve/verify results, stop and delete the pod on completion/cap. Record the cancellation and subsequent authorized replacement honestly in LabJournal.


Current local single-task authorization: the user requests local training only on a task the current model struggles with. Researchers execute WorkingMemory/SpatialTaskBattery/SingleTaskMotion from the verified restored-bias five-task checkpoint10000, preserving model weights, compatible Adam state, RNG and motion-family stream. Train only motion_duration_cued across unchanged D0/4/12/24, batch8, full BPTT/fp32, existing learning-rate groups and clipping; no anchors, architecture changes or curriculum. One local GPU worker with CPU threads at most2, under a new finite7200-second cap from the first profile including baseline/train/evaluation/report. Target4000 additional updates/32000 episodes, pin feasible exposure before production. Use scheduled validation and fresh paired parent/selected/terminal delay-specific tests. Task-only cross-entropy replaces the cloud's one-fifth task contribution; do not compensate learning rates or claim a causal gradient-conflict diagnosis. The current cloud five-task run, watcher and original deadline remain unchanged. No additional review gate or automatic cap extension.
